# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/

# Translations
*.mo
*.pot

# PyBuilder
target/

# pyenv
.python-version

# pipenv
# According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
# However, in case of collaboration, if having platform-specific dependencies or dependencies
# having no cross-platform support, pipenv may install dependencies that don't work, or not
# install all needed dependencies.
Pipfile.lock

# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/

# Environments
.env
.venv
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/
uv.lock
pyvenv.cfg
pg/
__pycache__/
llmgateway/
logs/
/db/
.claude/
/docs/
outputs/
# mypy
.mypy_cache/
.dmypy.json
dmypy.json

/.*/

# Keep root dot-directories ignored, except tracked GitHub Actions workflows.
!/.github/
/.github/*
!/.github/workflows/
/.github/workflows/*
!/.github/workflows/*.yml

# Node tooling for pinned frontend vendor bundles; commit their generated
# static artefacts and exact lock files, but never an install tree.
node_modules/
frontend/*/package-lock.json
!frontend/editor/package-lock.json
!frontend/topology/package-lock.json
!frontend/ui-runtime/package-lock.json

# Pyre type checker
.pyre/
.trae/
.playwright-cli/
AGENTS.md
MANAGER_PLAN.json
.cli-proxy/
/docs

# pytype static type analyzer
.pytype/

# Suggested docker volume
data/
/config/

# Cython debug symbols
cython_debug/
models_fallback_rules.mine.json
providers.local.json
providers.secret.json
models_*_rules.local.json
models_*_rules.secret.json
model_rotation.sqlite
*:Zone.Identifier
/scripts/fix_config_perms.sh
.cli-proxy/runtime/
